export interface Planet{
    name: string;
    images: any;
    description: string;
    distance: string;
    travel: string;
}

export interface Crewmate{
    bio: string;
    images: any;
    name: string;
    role: string;
}

export interface Technology{
    description: string;
    images: any;
    name: string;
}